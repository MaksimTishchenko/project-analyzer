# Python Project Analyzer

**Сервис статического анализа архитектуры Python-проектов**

---

## Описание проекта

Данный проект представляет собой сервис для **статического анализа Python-проектов**,
который по исходному коду строит **архитектурную модель проекта** и автоматически
генерирует **диаграммы классов**.

Анализ выполняется **без исполнения кода**, на основе
**AST (Abstract Syntax Tree)**, что делает подход безопасным,
воспроизводимым и независимым от окружения выполнения.

Проект ориентирован на **архитектурный анализ и обзор структуры кода**,
а не на проверку корректности выполнения программ.

---

## Кратко: как пользоваться сервисом

Сервис используется как **REST-приложение**.

Типовой сценарий работы:

1. Пользователь указывает путь к Python-проекту
   (локальному или GitHub-репозиторию).
2. Сервис выполняет **безопасное сканирование файлов проекта**.
3. Выполняется **статический анализ Python-кода через AST**.
4. Строится **внутренняя архитектурная модель проекта**.
5. Генерируется результат:

   * структурированный JSON-отчёт;
   * диаграмма классов (PlantUML или Mermaid).

Результаты возвращаются через HTTP API.

---

## Цель и задачи проекта

### Цель

Разработать сервис, позволяющий автоматически анализировать архитектуру
Python-проектов и визуализировать её в виде UML-подобных диаграмм.

### Основные задачи

* рекурсивное сканирование структуры проекта;
* статический анализ Python-кода с использованием AST;
* извлечение информации о модулях, классах, функциях и связях между ними;
* определение используемого стека технологий;
* генерация диаграмм классов в форматах **PlantUML** и **Mermaid**;
* предоставление результатов анализа через REST API.

---

## Общая архитектура

Проект построен как **модульный пайплайн статического анализа**:

```
FileScanner
    ↓
CodeParser (AST)
    ↓
TechStackAnalyzer
    ↓
DiagramGenerator / DiagramAI
    ↓
FastAPI (REST API)
```

Каждый модуль решает **одну логическую задачу**,
не зависит от деталей реализации других модулей
и может быть расширен или заменён без изменения всей системы.

---

## Принципы проектирования

* **Статический анализ**
  Код проекта не выполняется, используется только AST.

* **Устойчивость к ошибкам**
  Ошибки в отдельных файлах (например, `SyntaxError`)
  не прерывают анализ всего проекта.

* **Безопасность**
  Анализ файловой системы ограничен sandbox-директорией.

* **Модульность и расширяемость**
  GitHub-анализ и LLM подключаются как опциональные слои.

* **Production-ориентированность**
  Проект готов к контейнерному развёртыванию и конфигурации через ENV.

---

## Реализованные компоненты

### 1. FileScanner

Модуль отвечает за безопасный и устойчивый обход файлов проекта:

* рекурсивное сканирование директорий;
* игнор служебных каталогов (`.git`, `.venv`, `node_modules`, `.idea` и др.);
* поддержка `.gitignore` (через `pathspec` или fallback);
* пропуск бинарных файлов, симлинков и слишком больших файлов;
* сбор статистики сканирования (просмотрено / пропущено / причины).

Сбор статистики повышает **наблюдаемость** и упрощает диагностику поведения анализатора.

---

### 2. CodeParser (AST)

Модуль выполняет статический анализ Python-кода через стандартный модуль `ast`.

Извлекается следующая информация:

* модули проекта;
* классы и их базовые классы (наследование);
* методы классов и декораторы;
* функции верхнего уровня (включая `async`);
* импорты;
* атрибуты классов и экземпляров;
* эвристически определяемые связи **композиции и агрегации**.

Связи извлекаются на основе анализа:

* аннотаций типов;
* выражений присваивания (`self.x = Class()`),

с фильтрацией примитивных типов и дедупликацией связей.

Ошибки чтения или парсинга отдельных файлов
не прерывают анализ всего проекта.

---

### 3. TechStackAnalyzer

Модуль определяет используемый стек технологий на основе:

* импортов в AST;
* `requirements.txt`;
* `pyproject.toml` (Poetry).

Результат возвращается в виде структурированного JSON
и может использоваться для эвристической классификации типа проекта
(web / cli / scientific и др.).

---

### 4. Генерация диаграмм

Поддерживается автоматическая генерация диаграмм классов в форматах:

* **PlantUML**
* **Mermaid**

Диаграммы отображают:

* классы;
* наследование;
* композицию и агрегацию;
* методы (с возможностью фильтрации приватных).

Для сохранения читаемости предусмотрены:

* ограничение количества классов (`max_classes`);
* фильтрация элементов;
* группировка по модулям.

При ограничении числа классов используется эвристика «важности» класса,
учитывающая количество методов, наследование и связи,
что позволяет сохранить ключевые элементы архитектуры.

---

### 🤖 Использование LLM (опционально)

В проекте предусмотрена интеграция с LLM (Large Language Model)
в качестве **дополнительного уровня обработки результатов анализа**.

LLM **не участвует в анализе кода** и **не заменяет статический разбор AST**.

Последовательность работы:

1. Выполняется детерминированный статический анализ проекта.
2. Формируется базовая диаграмма классов.
3. При включённой LLM диаграмма может быть переработана
   для повышения читаемости (группировка, упрощение).
4. При ошибке, таймауте или некорректном ответе
   автоматически используется статический результат (fallback).

Таким образом:

* корректность анализа не зависит от LLM;
* результаты остаются воспроизводимыми;
* безопасность сервиса не нарушается.

---

### 5. REST API (FastAPI)

Сервис предоставляет REST API на базе FastAPI.

Основные эндпоинты:

* `GET /health`
* `POST /analyze/local`
* `POST /analyze/local/diagram`
* `POST /analyze/github`
* `POST /analyze/github/diagram`

Форматы ответов:

* `application/json` — структурные данные анализа;
* `text/vnd.plantuml` — PlantUML-диаграммы;
* `text/markdown` — Mermaid-диаграммы.

Swagger UI доступен по адресу:

```
/docs
```

---

## Типовой сценарий работы

### 1. Запуск сервиса (Docker)

```bash
docker compose up --build
```

Сервис доступен по адресу:

```
http://127.0.0.1:8000
```

---

### 2. Анализ локального проекта

`POST /analyze/local/diagram`

```json
{
  "path": "/workspace/mini_project",
  "diagram_format": "plantuml",
  "diagram_max_classes": 20
}
```

---

### 3. Результат анализа

В ответе возвращается:

* текст диаграммы классов;
* диаграмму можно визуализировать во внешнем рендерере.

Таким образом пользователь получает
**архитектурный обзор проекта без запуска кода**.

---

## Анализ GitHub-репозиториев

Поддерживается анализ публичных GitHub-репозиториев.

Реализация включает:

* shallow clone (`--depth 1`);
* анализ конкретного `ref` (branch / tag / commit);
* кэширование репозиториев с TTL;
* ограничения безопасности через конфигурацию.

GitHub-анализ является опциональным и может быть отключён.

---

## Безопасность локального анализа

Для защиты файловой системы используется sandbox-механизм:

* анализ разрешён только внутри `ANALYSIS_ROOT`;
* пути нормализуются и проверяются;
* попытка выхода за пределы sandbox приводит к HTTP 403.

---

## Итог

В рамках проекта реализован сервис, который:

* выполняет статический анализ Python-кода на основе AST;
* строит архитектурную модель проекта;
* автоматически генерирует диаграммы классов;
* предоставляет результаты через безопасный REST API;
* допускает расширение за счёт GitHub и LLM без нарушения детерминизма.

Проект может использоваться как учебный пример
статического анализа Python-проектов
и как основа для дальнейшего развития
(UI, метрики качества кода, расширенный архитектурный анализ).
